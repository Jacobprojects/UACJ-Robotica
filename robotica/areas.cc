#include "areas.hpp"

int circ( int R)

{
    return (R*pi);
}

float circ( float R, float b)

{
    return (R*pi);
}

int cuadro( int a)
{
    return(a*a);
}

float cuadro(float a, float c)
{
    return (a*a);
}

int cilindro(int ra, int h)
{
    return(pi*(ra*ra)*h);
}

float cilindro(float ra, float h, float t)
{
    return(pi*(ra*ra)*h);
}
